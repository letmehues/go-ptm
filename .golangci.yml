run:
  timeout: 5m
  tests: true

linters:
  disable-all: true
  enable:
    - gofmt
    - gosimple
    - govet
    - goimports
    - misspell
    - revive
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    - revive

linters-settings:
  revive:
    rules: